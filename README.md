Examples:

```
ml_fit  # fit with default config settings
ml_fit dataset=cifar100  # fit to different dataset
ml_fit model=resnet50  # fit different model
ml_fit train_loader.batch_size=512  # fit with different batch size
```
